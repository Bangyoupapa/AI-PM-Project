# 手機電池合規管理系統 — 專案說明文件

線上展示：https://ai-pm-project-topaz.vercel.app
GitHub：https://github.com/Bangyoupapa/AI-PM-Project

---

## 專案背景與問題

手機電池產品在出貨前必須通過多項國際法規認證，包含歐盟 RoHS（限制有害物質）、REACH（化學物質登錄）、UN38.3（鋰電池運輸測試）、IEC 62133（電池安全）、日本 PSE、中國 GB/T 31241 等。每一條法規都規定了電池中各種化學物質的最高含量限值，違規可能導致產品無法出口或被強制下架。

實際工作中，研發、採購、QA 三個團隊各自維護自己的 Excel 表格，法規 PDF 散落在各人電腦，合規狀態靠人工比對，到期提醒靠行事曆。這造成三個核心問題：第一，資訊孤島，三個團隊的資料無法即時同步；第二，人工比對費時且易出錯，每次稽核要翻找大量文件；第三，法規更新沒有系統性追蹤，工程師往往不知道法規已修訂。

---

## 解決方案概述

本系統是一個集中式的合規管理平台，讓三個團隊共用同一份資料來源。核心邏輯是：企業將料件（電池零件）的化學物質含量資料透過 Excel 批次匯入系統，AI 自動對照所有法規的物質限值，產生初步合規建議，工程師在系統上確認後即成為正式紀錄。法規文件 PDF 上傳後會被向量化，工程師可以用自然語言直接問法規問題，系統從文件中找出相關段落並生成回答。

---

## 六大功能模組

### 1. 法規管理

系統內建一個法規資料庫，管理員可以新增法規條目（名稱、代號、適用地區、版本號、到期日），上傳對應的 PDF 官方文件，並設定各種物質的限值（例如 RoHS 規定鉛含量不得超過 1000 ppm）。PDF 上傳後可手動觸發向量化，系統會將文件切割成段落並轉換為向量，存入 PostgreSQL 的 pgvector 擴充套件，供後續語意搜尋使用。

### 2. 法規動態 Feed

法規頁面頂端有一個動態 feed，提供兩種類型的資訊。第一種是到期提醒：系統每天計算所有法規的到期日，距到期 90 天以內顯示黃色警告，30 天以內顯示紅色危險警示。第二種是法規新聞：每天凌晨兩點，系統透過 Vercel Cron Job 自動呼叫 Tavily 搜尋 API，搜尋每條法規的最新新聞動態，每條法規保留最新三則，顯示在可捲動的卡片列表中。這讓團隊不需要主動查詢就能掌握法規動向。

### 3. 料件管理

料件即電池零件，例如電芯、電池管理系統（BMS）、外殼、接頭、電解液、隔離膜、負極、正極等。工程師可以手動新增單一料件，或透過 Excel 批次匯入。Excel 範本含有料號、料件名稱、類別、供應商、鉛含量（ppm）、鎘含量（ppm）、汞含量（ppm）、六價鉻含量（ppm）、是否含 SVHC 等欄位。匯入前系統會預覽每列的解析結果，有格式錯誤的列會標記出來並跳過，不影響其他列的匯入。

### 4. AI 自動合規分析

Excel 確認匯入後，系統立即呼叫 OpenAI GPT-4o-mini，對每一筆新匯入的料件與資料庫中所有法規進行交叉分析。分析依據包含料件的化學物質含量數據與各法規的物質限值設定。AI 輸出初步的合規狀態建議（合規、不合規、需要更多資訊）以及推理說明，這些建議在合規矩陣中以藍色 AI 標籤標示，工程師審閱後可一鍵確認或修改，確認後轉為正式紀錄。每筆料件的匯入使用獨立資料庫 transaction，確保單列失敗不影響其他列，並提供詳細的每列錯誤回報。

### 5. 合規矩陣

合規矩陣是一個二維表格，橫軸是法規，縱軸是料件，每個格子顯示該料件對該法規的合規狀態。狀態分為：合規、不合規、AI 建議待確認（藍色）、測試中、豁免、未知。點擊任何格子可以新增或編輯合規紀錄，包含測試日期、到期日、附件文件、備註等欄位。這讓整個產品線的合規狀況一覽無遺，稽核時可直接截圖使用。

### 6. AI 自然語言查詢（RAG）

工程師可以在 AI 查詢頁面用中文直接提問，例如「RoHS 對鉛的限值是多少」或「UN38.3 測試需要哪些項目」。系統將問題轉換為向量，在 pgvector 中找出最相關的文件段落（top-5），再將這些段落連同問題送給 GPT-4o-mini 生成回答。回答附帶引用來源，讓工程師可以確認資訊的出處段落。若尚未上傳法規 PDF 或未完成向量化，系統會以 mock 模式回應，不影響使用者體驗。

---

## 技術架構

### 前端與後端

使用 Next.js 16 App Router 架構，以 TypeScript strict mode 開發。頁面預設為 Server Component，只有需要互動的部分才加入 Client Component。API 邏輯全部集中在 `app/api/` 下的 route.ts 檔案。UI 元件使用 shadcn/ui 元件庫搭配 TailwindCSS v4。所有使用者輸入都透過 Zod schema 驗證後才進入業務邏輯。

### 資料庫

使用 PostgreSQL（部署在 Neon 雲端服務）搭配 pgvector 擴充套件處理向量搜尋。ORM 使用 Prisma 7。主要資料表包含：法規（Regulation）、法規文件（RegulationDocument）、文件向量段落（RegulationChunk）、物質限值（SubstanceLimit）、法規動態（RegulationAlert）、料件（Component）、供應商（Supplier）、合規紀錄（ComplianceRecord）、Excel 匯入紀錄（ComponentImportLog）。

### AI 與向量化

AI 功能統一透過 Vercel AI SDK 呼叫，底層使用 OpenAI GPT-4o-mini 進行文字生成，使用 text-embedding-3-small 進行向量化（1536 維）。向量化流程：PDF 解析文字 → 依詞數切割成段落（可設定每段大小與重疊詞數）→ embedMany 批次轉向量 → 寫入 pgvector。搜尋流程：問題文字 → embed → pgvector 餘弦距離搜尋 → 取 top-5 段落 → GPT 生成回答。

### 檔案儲存

法規 PDF 儲存在 Vercel Blob（私有存取），下載時由 API route 在伺服器端取得授權後串流回瀏覽器，不直接暴露 Blob URL。本機開發時若未設定 BLOB_READ_WRITE_TOKEN，自動 fallback 存到本地 `uploads/` 資料夾。

### 定時任務

使用 Vercel Cron Job 每天凌晨 02:00 觸發 `/api/cron/fetch-regulation-alerts`，呼叫 Tavily API 為每條法規搜尋最新新聞，並以 Bearer token（CRON_SECRET）保護路由避免未授權呼叫。

---

## 程式碼品質與測試

專案採用 Vitest 撰寫單元測試，共 32 個測試全數通過，涵蓋以下模組：

- **chunkText**：驗證文字切割邏輯，包含單一段落、多段落、重疊詞數（overlap）、空字串等情境
- **classifyExpiryUrgency**：驗證到期urgency分類邏輯，包含 null（無到期日）、超過 90 天、恰好 90 天（warning）、恰好 30 天（danger）、已過期等邊界情況
- **verdictWriter**：驗證 AI 建議與人工確認的狀態機轉換，確保合規紀錄狀態流轉正確
- **importRowSchema**：驗證 Excel 匯入的 Zod schema，包含有效料件、缺少必填欄位、無效類別等情況
- **ComponentCategory**：驗證 enum 完整性，確保所有料件類別都有對應的中文標籤

架構上將純函式從業務邏輯中分離（如 `chunkText`、`classifyExpiryUrgency`、`verdictWriter`），使其可獨立測試而不需要資料庫連線。

---

## 部署環境

應用部署在 Vercel，資料庫使用 Neon PostgreSQL，檔案儲存使用 Vercel Blob。需要的環境變數包含：`DATABASE_URL`（必填）、`OPENAI_API_KEY`（AI 功能需要）、`BLOB_READ_WRITE_TOKEN`（選填，無則存本地）、`TAVILY_API_KEY`（選填，無則不顯示新聞）、`CRON_SECRET`（Vercel 部署需要）。若沒有 OpenAI API key，設定 `MOCK_AI=true` 可讓系統以規則為基礎的方式運作，所有功能頁面仍可正常使用。

---

## 預設資料

系統 seed 後自動建立七條法規：RoHS（歐盟限制有害物質指令）、UN38.3（鋰電池運輸測試規範）、IEC 62133（攜帶式鋰電池安全標準）、CE（歐盟 CE 認證）、REACH（歐盟化學物質登錄評估許可）、PSE（日本電器安全法）、GB/T 31241（中國攜帶式電子產品鋰電池安全要求）。

---

## 可延伸方向

供應商入口：讓供應商自行登入上傳 SDS 安全資料表與合規文件，減少採購人工索件的工作量。合規歷史版本追蹤：記錄每次狀態變更的時間與操作者，提供完整的 audit trail 供稽核使用。法規變更通知：當法規新聞出現關鍵字（如「修訂」、「amendment」）時，自動發送 Email 或 Slack 通知相關人員。多語言支援：新增英文介面，供應商或海外分公司可使用。
